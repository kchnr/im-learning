
    book(game_of_thrones, george_rr).
    book(clash_of_kings, george_rr).
    book(storm_of_swords, george_rr).
    book(feast_of_crows, george_rr).
    book(gate_of_fires, steven_pressfield).

    author(Y) :- book(X, Y).
